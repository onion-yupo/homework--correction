# 计算营 AI Copilot — 产品功能文档

> 更新时间：2026-04-30（飞书 OAuth 登录 + 系统内收作业表单 + 入营问卷 + 去掉多维表格依赖）
> 文档性质：产品功能设计归档文档，记录当前版本的系统定位、功能架构与详细设计

---

## 一、系统定位

### 1.1 一句话定义

在计算营 1 对多的真实业务约束下，用 AI 托住老师最重、最重复、最难规模化但又最影响服务体验的工作，让老师更接近交付"1 对 1 私教感"的服务体验。

### 1.2 命名

**计算营 AI Copilot**

"副驾驶"的概念精准对应当前定位：AI 不替代老师，而是坐在老师旁边，帮老师处理最重、最重复的工作，让老师专注于真正需要判断和温度的地方。系统覆盖的服务远不止批改——从学情收集、每日作业服务、每周监测汇总到结营总结，贯穿整个服务周期。

### 1.3 核心价值

系统同时回应两个角色的价值缺口：

| 角色 | 核心痛点 | 系统提供的价值 |
|------|---------|--------------|
| 辅导老师 | 高重复、长期缠身的服务劳动难以解放 | 自动批改 + 错因诊断 + 反馈初稿生成，把老师从"组织语言"的苦差事中解放出来 |
| 家长 | 服务价值感薄弱，尤其全对时服务显得单薄 | 每次作业都有结构化反馈，全对也有拔高建议和加练题，阶段性学情报告让服务价值可感知 |

### 1.4 设计原则

1. **AI 辅助，老师把关**：AI 做预处理和初稿生成，老师做最终确认和修改，确保 100% 准确
2. **面向家长的输出以真人为主**：AI 在幕后赋能，家长感知到的是老师的专业服务
3. **不增加家长负担**：家长侧的操作越轻越好，结构化负担由系统和老师承担
4. **数据持续沉淀**：每次批改的结果不是用完即弃，而是沉淀为学生档案，驱动后续更精准的服务

---

## 二、服务周期分层

系统按计算营的服务生命周期分为四个阶段：

```
承接期（入营前）→ 行课期·每日 → 行课期·每周 → 结束期（结营）
```

### 2.1 承接期：学情收集与建档

**目标**：在服务开始前建立学生基线档案，让后续服务有据可依。

| 功能 | 说明 | 当前状态 |
|------|------|---------|
| 入营问卷（家长填写） | 老师发送专属链接，家长填写：孩子基本信息、当前水平、薄弱点、家长关注点 → 系统自动建档 | 已规划，本次实现 |
| 学生建档 | 家长提交入营问卷后，系统自动创建学生档案（students + enrollments + student_profiles 三表） | 已规划，本次实现 |
| 分班测试录入 | 测试结果结构化回流到系统 | 规划中 |

### 2.2 行课期 · 总体设计

#### 数据粒度

| 粒度 | 含义 | 典型场景 |
|------|------|---------|
| **个人** | 单个学生单次作业 | 作业批改、加练题、反馈话术 |
| **个人汇总** | 单个老师负责的某个班下所有学生的汇总 | 学生状态管理（每个学生 21 天的提交/复核/反馈状态）、周学习报告/周错题汇总/个人学情复盘的发送状态 |
| **单老师全班** | 单个老师负责的同一个班的整体数据 | 当日全班汇总、周日综合测试全班汇总、全班学情复盘 |
| **全老师全班** | 所有老师负责的同一期不同班的汇总 | 主讲教研数据（按 level 聚合所有班的错题数据） |

#### 四种仪表盘

| 仪表盘 | 使用者 | 视角 | 核心内容 |
|--------|-------|------|---------|
| **A：个人作业详情** | 辅导老师 | 单个学生单次作业 | 批改复核、反馈建议生成（即当前已实现的批改系统详情页） |
| **B：班级学生管理** | 辅导老师 | 某个班下所有学生 | 每个学生每天的作业状态（提交/未交、是否复核、是否反馈）；周学习报告、周错题汇总、个人学情复盘是否已发送给家长 |
| **C：班级服务管理** | 辅导老师 | 同一期不同班 | 各班的当日全班汇总、周日综合测试全班汇总、全班学情复盘的内容跳转和发送状态 |
| **D：全局数据看板** | 主讲老师 | 同一期所有班 | 按 level 聚合的主讲教研数据（高频错因、典型错题），老师自取，无需推送 |

#### 承载平台选择原则

所有功能均在批改系统内闭环，不依赖飞书多维表格。老师深度交互（复核、编辑、调整）在批改系统完成；静态资源（图片、PDF、视频）由系统生成后，老师在系统内导出再发送给家长。最终给家长的交付物以微信环境下可直接查看的形式为主（文字、图片、PDF、语音、视频），不依赖外部链接。

### 2.3 行课期 · 每日维度

**目标**：围绕每天的作业，完成"批改 → 诊断 → 反馈 → 沉淀"的完整闭环。这是系统的**核心主战场**，详见第三章。

> 注：计算营共 21 天（3 周），周一~周六为单一知识点日常练习，周日为综合训练。此外第 22/23/24 天分别对应第一/二/三周易错点加练。目前所有天数的服务流程一致，后续可能在反馈内容上做差异化设计。

#### 面向家长的服务

| # | 服务内容 | 粒度 | 输出形式 | 承载平台 | 仪表盘 | 当前状态 | 说明 |
|---|---------|------|---------|---------|--------|---------|------|
| 1 | **作业批改** | 个人 | 原作业标注对错的图片 | 批改系统生成 → 老师截图发微信 | A | 已实现 | 直接在作业原图上叠加对错标注 |
| 2 | **个性化加练题** | 个人 | 题目图片 | 批改系统生成导出 → 老师发微信 | A | 已实现 | 基础/巩固/挑战三档，老师勾选后导出图片 |
| 3 | **个性化反馈话术** | 个人 | 文字 | 批改系统生成 → 老师复制发微信 | A | 已实现 | 整体评价 + 错因分析 + 鼓励引导，老师可编辑调整 |
| 4 | **当日全班汇总** | 单老师全班 | 视觉图片 | 多维表格（接服务生成，回显管理） | C | 规划中 | 按学期群发布，含全班正确率分布、高频错题/知识点、量化数据 |

#### 面向老师的工具

| # | 服务内容 | 粒度 | 输出形式 | 承载平台 | 仪表盘 | 当前状态 | 说明 |
|---|---------|------|---------|---------|--------|---------|------|
| 5 | **批改复核** | 个人 | 网页交互 | 批改系统 | A | 已实现 | 确认/修改 AI 判定，筛选错题，隐藏题目 |
| 6 | **反馈建议生成** | 个人 | 网页交互 | 批改系统 | A | 已实现 | 四模块结构，支持编辑和 AI 重新生成 |
| 7 | **学生状态管理** | 个人汇总 | 网页 | 批改系统 | B | 部分实现 | 任务看板展示所有学生 × 24 天的提交/复核/反馈状态；周报/学情复盘等发送状态后续跟进 |

### 2.4 行课期 · 每周维度

**目标**：将每日数据汇总为周维度的学情洞察，服务家长和主讲老师。

#### 面向家长的服务

| # | 服务内容 | 粒度 | 输出形式 | 承载平台 | 仪表盘 | 当前状态 | 说明 |
|---|---------|------|---------|---------|--------|---------|------|
| 8 | **周学习报告** | 个人 | 图片/PDF | 批改系统生成 PDF → 老师发微信 | B | 已实现 | 本周正确率趋势、进步知识点、错题回顾、下周建议，A4 分页设计，支持 PDF 导出 |
| 9 | **周错题汇总** | 个人 | 图片/PDF | 批改系统生成 → 老师发微信 | B | 规划中 | 本周个人错题汇编，方便孩子针对性复习巩固 |
| 10 | **周日综合测试全班汇总** | 单老师全班 | 视觉图片 | 批改系统生成 → 老师导出发学期群 | C | 规划中 | 素材为周日综合测试，可对比日常 vs 综合表现 |

#### 面向主讲老师的服务

| # | 服务内容 | 粒度 | 输出形式 | 承载平台 | 仪表盘 | 当前状态 | 说明 |
|---|---------|------|---------|---------|--------|---------|------|
| 11 | **主讲教研数据** | 全老师全班 | 数据报告 | 批改系统学情分析页 | D | 规划中 | 按 level 聚合所有班数据：高频错因知识点 TOP N、典型错题。提前给主讲备课周六直播课 |

### 2.5 结束期（21 天维度）：学情复盘与续费转化

**目标**：生成整期学情报告，作为服务成果的最高潮交付物，同时自然支撑续报。

| # | 服务内容 | 粒度 | 服务对象 | 输出形式 | 承载平台 | 仪表盘 | 当前状态 | 说明 |
|---|---------|------|---------|---------|---------|--------|---------|------|
| 12 | **学情复盘** | 个人 | 家长（点对点） | 视频（高规格） | 批改系统生成视频 → 老师发微信 | B | 规划中 | 整期进步曲线、知识点掌握全景、高光时刻、下阶段建议。最终目的：续费转化 |
| 13 | **学情复盘** | 单老师全班 | 家长群 | 视频/长图 | 批改系统生成 → 老师导出发学期群 | C | 规划中 | 班级整体数据、进步之星、共性成长点，增强集体荣誉感和服务价值感 |

---

## 三、每日作业页面 — 详细设计

### 3.1 页面整体布局

采用"上-左-右"三栏结构：

```
┌──────────────────────────────────────────────────────────────┐
│  计算营 AI Copilot                              [返回列表]   │
├──────────────────────────────────────────────────────────────┤
│  段喆 · [二年级上][二年级下]                                  │
│  [1●●][2][3●●][4][5][6●●]...[18●●][19][20][21]              │
│       ↑ 天数导航，●● = 核对/反馈状态小点                      │
├────────────────────────┬─────────────────────────────────────┤
│                        │  统计条（总题数/正确/错误/正确率）      │
│  左栏                   ├─────────────────────────────────────┤
│  作业图片 + 标注叠加     │  Tab 栏：[批改复核][反馈建议]        │
│  [全部显示/全部隐藏]     │         [未核对][未反馈]             │
│  [查看大图]             ├─────────────────────────────────────┤
│                        │  Tab 内容区（模块化卡片）              │
│                        │                                     │
├────────────────────────┴─────────────────────────────────────┤
```

左右栏之间支持拖拽调整比例。

### 3.2 上栏：学生上下文 + 天数导航

**学生信息**：

- **学生姓名**：如"段喆"
- **学期切换**：如果该学生有多个学期的数据，姓名旁显示学期切换按钮（如 `[二上]` `[二下]`），点击切换后加载对应学期的天数数据。学期统一使用简写格式，按年级 + 上下自动排序

**天数导航**：

- 顶部固定显示 1-24 天按钮（1-21 日常 + 22/23/24 易错点加练），有作业的天数高亮可点击，当前天数特殊标记
- 每个天数按钮下方显示**状态小点**：两个小点分别代表"核对"和"反馈"状态（绿色=已完成，灰色=未完成）
- 如果某一天有多个 jobId（多次提交），则显示多行小点
- 没有 jobId 的天数不显示小点
- 同天多次提交时，作业原图区域显示序号切换器（1、2、3...）

### 3.3 左栏：作业图片 + 标注

- 展示作业原图
- 叠加 AI 批改的对错标注框（绿色=正确，红色=错误）
- 标注框右上角有小图标（✓ 或 ✗），尺寸 12px，不遮挡原始内容
- 点击标注框 → 右侧面板自动滚动到对应答案并高亮
- **人工标注**：老师可切换到"人工标注"模式，在图片上框选 AI 未识别到的题目区域并标记对/错。人工标注与系统标注视觉外观完全一致（家长截图无法区分），且计入总题数和正确率统计。点击已有人工标注可弹出菜单（正确/错误/删除）进行调整。人工标注模式下，AI 标注降低透明度以避免干扰
- **全部显示/全部隐藏按钮**：一键控制所有标注的显示状态，方便老师直接截图
- 图片通过 Canvas 重绘消除 EXIF 旋转，确保标注坐标对齐

**大图查看模式**：

- 点击"查看大图"打开全屏浮层，图片撑满容器，标注精确覆盖
- 大图模式下点击单个标注可隐藏/显示该标注
- 支持全部显示/全部隐藏操作
- 主视图中点击标注仍为"跳转到对应题目"（与大图模式的"隐藏标注"交互区分）

### 3.4 右栏 — 统计条 + 状态标记

**统计条**（固定头部，Tab 切换不影响）：

```
┌──────────┬──────────┬──────────┬──────────┐
│  40      │  37      │  3       │  93%     │
│  总题数   │  正确     │  错误     │  正确率   │
└──────────┴──────────┴──────────┴──────────┘
```

**Tab 栏 + 状态标记**：

- Tab 栏：`[批改复核]` `[反馈建议]`，采用卡片式大块按钮设计（圆角边框 + 填充背景），选中态主色高亮，视觉上呈现为两个独立区块
- Tab 旁显示当前提交的工作流状态按钮：`[未核对/已核对]` `[未反馈/已反馈]`
- 老师点击可切换状态，状态持久化到后端，同步更新天数导航的小点颜色

### 3.5 右栏 — Tab 1：批改复核

**定位**：老师确认和修改 AI 批改结果的工作区。

#### 筛选栏（置顶，不随滚动消失）

```
[全部(40)] [错题(3)] [正确(37)] [已展示(38)] [已隐藏(2)]
```

- 每个筛选项后跟数量
- 支持快速筛出错题、隐藏题等

#### 内容结构

- **大题标题分隔**：如"1. 改正下面的算式。""2. 口算。""5. 比大小。"
- **子题平铺展示**：每个子题可展开/折叠（默认展开）
- 每个答案卡片包含：
  - 手写答案（AI 识别结果，支持 KaTeX 公式渲染）
  - **判定按钮组**（`✓ 正确` / `✗ 错误` 双按钮，醒目可点击，老师可切换）
  - 正确答案（API 返回时展示，支持 KaTeX 公式渲染）
  - 知识点标签
  - 答案分析（支持 KaTeX 公式渲染）
  - **隐藏/展示切换**：每道题卡片上有"展示中/已隐藏"按钮，隐藏后视觉弱化（降低透明度）但不从列表消失，同时左侧图片对应标注也联动弱化

#### 交互

- 老师逐题扫一遍，确认或修改判定
- 修改判定后，统计条实时更新
- 点击图片标注可定位到对应答案

### 3.6 右栏 — Tab 2：反馈建议

**定位**：AI 基于批改结果自动生成的服务内容，老师审阅后可直接编辑调整或复制发给家长。

**触发机制**：进入批改详情页即自动预加载反馈建议（无需等待切换 Tab），生成基于老师最终判定结果 + 作业原图（多模态）。

**技术实现**：调用火山引擎 Ark（豆包大模型 doubao-seed-2-0-lite，多模态图文输入，`reasoning_effort: minimal` 关闭深度思考加速响应），流式输出 Markdown，前端通过 marked + KaTeX 实时渲染。

#### 统一四模块结构

不再按正确率分三档采用不同模块组合，而是统一固定四个模块，由模型根据实际批改数据自行调整内容深度和语气：

| 模块 | 说明 |
|------|------|
| **一、整体评价** | 根据正确率自动调整语气；融入书写观察和修改痕迹分析 |
| **二、作业分析** | 全对时→掌握确认（哪些知识点扎实）；有错题时→逐题分析错因+纠正建议。不管什么情况都输出 |
| **三、家长反馈话术** | 可直接发给家长的完整文案，提到已出三组加练题供选择 |
| **四、加练题目** | 固定三个难度层级：基础巩固 / 巩固加练 / 挑战加练，每层 3-5 道题。模型根据表现自动标注【推荐】 |

#### 模块 1：整体评价

- 根据实际正确率自动调整语气（全对→热情肯定，少量错误→积极鼓励，较多错误→温暖鼓励先肯定做对的部分）
- **观察作业图片中学生手写的部分**，自然融入对书写的观察
- **重点关注修改/涂改痕迹**：如果图片中有答案被涂改、擦除、覆盖书写的痕迹，指出原答案和修改后答案。修改后正确→肯定自我检查意识；修改后仍错→分析原因

#### 模块 2：作业分析

- **全对时**：掌握确认——具体指出哪些知识点掌握扎实，如有修改痕迹则肯定自我检查习惯
- **有错题时**：逐题分析——题目内容、学生答案、正确答案、具体错因、纠正建议。如有修改痕迹，指出原答案和修改后答案
- 不管哪种情况，都对核心知识点做简要掌握情况总结

#### 模块 3：家长反馈话术

- 可直接复制发送的完整文案
- 结构：肯定做得好的方面 → 书写习惯观察 → 错题和原因（如有）→ 已出三类加练题供选择 → 鼓励
- 话术里提到"已根据不同难度出了三组练习题，您可以根据孩子情况选择合适的"

#### 模块 4：加练题目

> **关键洞察**：家长最看重的不是批改结果本身，而是"基于错题出加练题"。这是家长最愿意为之付费/留存的核心价值点。

三个难度层级统一输出，模型根据学生表现在最适合的层级标注【推荐】：

| 层级 | 定位 | 题量 |
|------|------|------|
| 基础巩固加练 | 难度略低于本次作业，夯实基本功 | 3-5 道 |
| 巩固加练 | 与本次作业同等难度，针对涉及知识点巩固 | 3-5 道 |
| 挑战加练 | 拔高题或思维拓展题，适合冲刺 | 3-5 道 |

**加练拼盘功能**：

- 三个难度层级合并为一个"加练题目"模块展示
- 每道题有勾选框，老师可以选中/取消选中单道题
- 支持按难度层级全选/取消全选
- 题目和答案在页面上合并展示（方便老师审阅），导出时只包含题目
- **图片导出**：选中的题目可一键导出为图片（复制到剪贴板或下载 PNG），方便发给家长

#### 老师二次调整能力

每个模块卡片（整体评价、作业分析、家长反馈话术、加练题目）支持两种调整方式：

**直接编辑**：
- 点击"编辑"按钮，内容区变为可编辑的 Markdown 文本框
- 老师可以直接修改措辞、删减内容、补充信息
- 保存后重新渲染并持久化到后端

**AI 重新生成**：
- 点击"重新生成"按钮，展开补充指令输入框
- 老师可以输入补充指令（如"语气更温和一些""这道题的错因应该是进退位问题"）
- 确认后只重新生成该模块，其他模块不受影响
- 流式替换该模块内容，完成后自动持久化

#### Prompt 质量控制

**禁词规则**（严格遵守）：
- 笼统归因类：不认真、粗心、马虎、大意、敷衍等
- 否定能力类：笨、基础太差、学不会等
- 主观态度类：懒、懒惰、偷懒等
- 贬低错误类：低级错误、不该错等
- 负面评判类：没救了、差劲、屡教不改等

**表述规则**：
1. 所有分析只描述行为与知识点问题，不评价性格、态度、天赋与智商
2. 语言客观、具体、可落地，不模糊、不指责
3. 无法判断具体错因时，用"需要进一步确认"代替笼统归因

**错因分析标准示范**：
- 看错数字（如 36 看成 63）→ 错因："审题时对数字顺序识别存在混淆"；纠正建议："读题时采用笔尖指读的方式逐位认读，做题前圈画关键数字"
- 数字混淆（如 0 和 6）→ 错因："对数字 0 和 6 的字形特征区分不够清晰"；纠正建议："书写时强化字形差异，认读时适当放慢速度"

#### UI 呈现

- **模块化卡片布局**：每个模块一张独立卡片，有标题栏和内容区
- 每张卡片标题栏有**复制**、**编辑**、**重新生成**三个操作按钮
- 加练题目卡片内按难度分组，每组有推荐标记和全选/取消按钮
- 数学公式通过 KaTeX 渲染（`$...$` 语法）
- 流式输出时，最后一个卡片显示闪烁光标，之前的卡片已完成
- 反馈内容持久化到服务端，刷新页面后自动加载

---

## 四、作业提交与批改链路

### 4.1 提交入口设计

采用**系统内收作业表单 + 专属链接置顶**方案，完全在批改系统内闭环：

- 老师从任务看板为每个学生生成**永久专属提交链接**（HMAC-SHA256 签名 token，编码 studentId + teacherId + term）
- 链接示例：`https://域名/homework/submit/:token`
- 专属链接置顶在微信群/私聊中，家长每天点击即可提交
- 家长侧操作：点链接 → 上传作业图片（视觉 API 自动识别学期/天数）→ 提交 → 看到"已收到"状态，最轻量
- 无需飞书账号，无需任何注册登录

### 4.2 批改链路（系统内闭环）

```
家长点击专属链接 → 系统内收作业表单（/homework/submit/:token）
    ↓
POST /api/submit/:token（验证 token + 接收图片）
    ↓
视觉 API 自动识别学期/天数（可手动修改）
    ↓
进入批改队列（复用现有串行队列逻辑）
    ↓
腾讯云试题批改 Agent → 结构化结果写入 SQLite + JSON 文件
    ↓
家长页面显示"老师正在批改中" / 批改完成提示
    ↓
老师在任务看板看到新提交 → 打开批改详情页 → 复核 + 生成反馈 → 标记已反馈
```

**完全自闭环**：整条链路不依赖飞书表单、多维表格、Flow A/B 或任何第三方自动化工具，故障点最少，数据全部在系统内可查。

### 4.3 作业状态流转

一份作业从提交到完成，经历以下状态：

```
未交 → 已交待批 → 处理中 → 已完成 / 处理失败
                              ↓
                    老师点击链接复核 → 标记已核对 → 生成反馈 → 标记已反馈 → 发给家长
```

---

## 五、数据流转与沉淀

### 5.1 每日数据流

```
家长点击专属提交链接（/homework/submit/:token）→ 上传作业图片
    ↓
POST /api/submit/:token → 验证 token → 视觉识别学期/天数 → 加入批改队列
    ↓
批改系统后台：腾讯云 API → 结构化批改结果 → 写入 SQLite + JSON 文件
    ↓
老师在任务看板看到新提交通知
    ↓
老师打开批改详情页 → Tab 1 批改复核 → 确认/修改判定 → 标记已核对
    ↓
Tab 2 反馈建议 → 豆包大模型流式生成六模块反馈 → 老师编辑调整
    ↓
老师复制加练题/家长话术 → 发给家长 → 标记已反馈
```

### 5.2 学生档案累积

| 数据维度 | 来源 | 用途 |
|---------|------|------|
| 每日正确率 | 批改结果 | 趋势图、周报、整期报告 |
| 错题记录 | 批改结果 + 老师确认 | 错题清理、加练推荐 |
| 错因分类 | AI 诊断 + 老师修改 | 错因分布分析、针对性建议 |
| 知识点掌握 | API 返回 + 累积统计 | 薄弱点追踪、拔高判断 |
| 老师备注 | 老师手动输入 | 个性化服务参考 |
| 作业完成情况 | 提交记录 | 出勤统计、未交提醒 |
| 核对/反馈状态 | 老师标记 | 工作流追踪 |

### 5.3 数据驱动的服务增强

- **整体评价**引用历史数据："比上次正确率提升 8%""这个知识点连续 3 天全对"
- **错因诊断**结合历史："余数概念已经是第 3 次出错，建议重点巩固"
- **加练推荐**基于错因画像而非单次结果
- **周报/结营报告**从累积数据自动生成

---

## 六、技术现状

### 6.1 已实现（当前版本）

| 能力 | 实现方式 |
|------|---------|
| 图片上传 + EXIF 消除 | 前端 Canvas 重绘 + 分辨率缩放（最大长边 1600px），统一为方向正确的 JPEG |
| AI 批改 | 腾讯云试题批改 Agent API（SubmitQuestionMarkAgentJob + DescribeQuestionMarkAgentJob） |
| 批改结果渲染 | 左侧图片标注叠加 + 右侧结构化详情展示 |
| 老师修改判定 | 双按钮组（✓ 正确 / ✗ 错误）切换，实时更新统计，修改后持久化到后端，刷新页面保持 |
| 人工标注 | 老师可框选 AI 未识别的题目标记对/错，视觉与系统标注完全一致（家长无法区分），计入统计；点击可切换对错/删除 |
| 标注定位联动 | 点击图片标注 → 右侧滚动到对应答案并高亮 |
| 大图查看 | 全屏浮层，标注精确覆盖，支持逐个/全部隐藏标注 |
| 标注显隐控制 | 主视图和大图模式均支持全部显示/全部隐藏 |
| 左右分栏拖拽 | 可调整比例，默认根据图片宽高比自动适配 |
| 大题标题分隔 | 批改详情中按大题分组展示，保持平铺逻辑 |
| 批改复核筛选栏 | 全部/错题/正确/已展示/已隐藏 + 数量，置顶不随滚动消失 |
| 题目隐藏/展示 | 每道题卡片上有隐藏/展示切换，隐藏后答案内容弱化但操作按钮保持清晰，联动左侧标注，状态持久化到后端 |
| AI 反馈建议 | 豆包大模型（doubao-seed-2-0-lite，多模态图文输入，`reasoning_effort: minimal`）流式生成，统一四模块结构（整体评价/作业分析/家长反馈话术/加练题目） |
| 反馈模块编辑 | 每个模块支持直接编辑 Markdown 原文，保存后重新渲染并持久化 |
| 反馈模块重新生成 | 每个模块（含加练题目）支持补充指令后单独调用 AI 重新生成，其他模块不受影响 |
| 数学公式渲染 | marked + KaTeX，增强的公式验证、LLM 输出清洗、渲染失败优雅降级 |
| 模块化卡片 | 反馈按 ### 标题切分为独立卡片，每个可复制/编辑/重新生成 |
| 加练拼盘 | 三个难度合并展示，支持单题勾选，题目+答案合并展示 |
| 加练导出 | 选中题目导出为图片（复制到剪贴板或下载 PNG），只含题目不含答案 |
| 禁词机制 | prompt 中严格约束禁止笼统归因、否定能力等表述 |
| 修改痕迹识别 | prompt 指导模型观察图片中的涂改痕迹，在反馈中体现 |
| 学期切换 | 同一学生多个学期数据时，支持切换学期查看 |
| 天数导航状态 | 每天的 tab 下显示核对/反馈状态小点（绿色=完成，灰色=未完成） |
| 核对/反馈标记 | 单次提交级别的"已核对/已反馈"状态，持久化到后端 |
| 反馈持久化 | 生成的反馈内容保存到服务端，刷新页面后自动加载 |
| jobId 隔离 | 切换 job 时 AbortController 中止旧请求，防止数据混淆 |
| 复制按钮 HTTP 降级 | navigator.clipboard 不可用时自动降级为 textarea + execCommand |
| 结果持久化 | 批改结果 + 图片存储到服务器文件系统 |
| URL 直达 | 通过 ?jobId=xxx 打开已批改的作业，加载学生信息 |
| 系统内收作业表单 | 家长通过专属 token 链接（/submit/:token）直接提交作业图片，无需飞书/任何第三方账号 |
| 入营问卷 | 家长通过 token 链接（/enroll/:token）填写学生基线档案，自动写入 students + enrollments + student_profiles |
| 飞书 OAuth 登录 | 老师用飞书账号登录（JWT Cookie，7 天有效期），路由守卫保护所有老师页面 |
| 数据底座（SQLite） | teachers / students / enrollments / jobs / answers / reports / teacher_preferences / student_profiles 八张表，支持多维度交叉查询 |
| 任务看板（工作台） | 三区域布局（任务总览/待办任务/班课学生看板），学生×24天状态矩阵，冻结/提醒/排序偏好持久化 |
| 服务天数与期起始日期 | 顶部金色标签「服务期 第X/24天」，期起始日期可设置并持久化，未提交统计和提醒文案均基于当前服务天数 |
| 学情分析 | 个人正确率趋势 + 知识点掌握 + 错题列表；全班概览（6模块：统计卡片/每日趋势/高频错题/知识点全景/学生排行/AI教研建议） |
| 周学习报告 | A4 分页设计（5模块），LLM 流式生成（`reasoning_effort: 'low'`），支持补充指令重新生成 + PDF 导出（Puppeteer 服务端渲染） |
| 学期总结视频 | Remotion + FFmpeg + 火山引擎 TTS，6 个场景 + 逐句字幕 + BGM |

### 6.2 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3.5 + Vite 6 + Pinia + Vue Router 4 + SCSS |
| 公式渲染 | KaTeX 0.16 + marked（增强的公式提取、验证、降级渲染） |
| 图片导出 | html2canvas（加练题目导出为图片） |
| 后端 | Hono 4（Node.js）+ tsx |
| AI 批改 | 腾讯云 tencentcloud-sdk-nodejs-ocr |
| AI 反馈 | 火山引擎 Ark · 豆包大模型（doubao-seed-2-0-lite，多模态图文输入，流式输出，`reasoning_effort: minimal`；图片经 sharp 压缩至长边 1200px/JPEG quality 70 后送入） |
| AI 周报 | 火山引擎 Ark · 豆包大模型（doubao-seed-2-0-lite，`reasoning_effort: 'low'`，流式输出并过滤 reasoning_content） |
| 认证 | 飞书 OAuth 2.0（企业自建应用）+ JWT Cookie（httpOnly，7天有效期，jsonwebtoken） |
| 数据存储 | SQLite（better-sqlite3，结构化数据底座）+ 服务器文件系统（jobs JSON + images + feedback） |
| 部署 | PM2 + Nginx（OpenResty） |

### 6.3 API 接口

| 接口 | 方法 | 调用方 | 说明 |
|------|------|--------|------|
| `/api/correction/submit` | POST | 前端 | 上传图片文件，返回 jobId，前端自行轮询 |
| `/api/correction/submit-url` | POST | 外部系统 | 传图片 URL + 学生元数据，同步等待批改完成返回结果 + 链接 |
| `/api/correction/submit-async` | POST | 兼容保留 | 异步批改（旧接口，兼容外部系统调用，新提交请用 /api/submit/:token） |
| `/api/correction/submit-file` | POST | 外部系统 | 文件上传提交（同步） |
| `/api/correction/query` | GET | 前端 | 按 jobId 查询批改状态 |
| `/api/correction/result/:jobId` | GET | 前端 | 获取已持久化的批改结果 |
| `/api/correction/image/:jobId` | GET | 前端 | 获取已存储的作业原图 |
| `/api/feedback/generate` | POST | 前端 | 传批改数据，流式返回反馈 Markdown |
| `/api/feedback/regenerate-module` | POST | 前端 | 单模块重新生成，传模块标题+补充指令，流式返回 |
| `/api/feedback/save` | POST | 前端 | 保存反馈内容 |
| `/api/feedback/load/:jobId` | GET | 前端 | 加载已保存的反馈 |
| `/api/jobs` | GET | 前端 | 作业列表（自动刷新 PROCESSING 状态 + 队列推进） |
| `/api/jobs/:jobId/delete` | POST | 前端 | 删除作业记录 |
| `/api/jobs/:jobId/flags` | POST | 前端 | 更新核对/反馈状态标记 |
| `/api/jobs/by-student` | GET | 前端 | 按学生查询，返回按学期分组的天数数据 |
| `/api/vision/analyze` | POST | 前端 | 视觉识别学期 + 天数 |
| `/api/dashboard/matrix` | GET | 前端 | 学生×天数状态矩阵（按老师×期） |
| `/api/dashboard/overview` | GET | 前端 | 今日概览统计 |
| `/api/dashboard/sort-prefs` | GET/POST | 前端 | 获取/保存排序和时间筛选偏好 |
| `/api/dashboard/freeze` | POST | 前端 | 冻结/解冻任务 |
| `/api/dashboard/remind` | POST | 前端 | 生成提醒家长文案（流式，传入 currentDay） |
| `/api/dashboard/term-start` | GET/PUT | 前端 | 获取/设置当前期的起始日期 |
| `/api/teachers` | GET | 前端 | 获取所有老师列表 |
| `/api/teachers/:id/enrollments` | GET | 前端 | 获取某老师负责的所有 level 和学生 |
| `/api/analytics/student-trend` | GET | 前端 | 个人正确率趋势 |
| `/api/analytics/student-errors` | GET | 前端 | 个人错题列表 |
| `/api/analytics/student-knowledge` | GET | 前端 | 个人知识点掌握情况 |
| `/api/analytics/class-overview` | GET | 前端 | 全班概览统计 |
| `/api/analytics/class-knowledge-top` | GET | 前端 | 全班知识点错误率 TOP N |
| `/api/analytics/class-error-questions` | GET | 前端 | 全班高频错题 |
| `/api/analytics/class-teaching-advice` | POST | 前端 | AI 教研建议流式生成 |
| `/api/report/generate-weekly` | POST | 前端 | 周报 LLM 流式生成（reasoning_effort: low） |
| `/api/report/weekly` | GET | 前端 | 获取已保存的周报 |
| `/api/report/status` | GET | 前端 | 批量查询报告生成状态 |
| `/api/report/export-pdf` | GET | 前端 | 服务端 Puppeteer 生成 PDF 下载 |
| `/health` | GET | 运维 | 健康检查 |

---

## 七、认证与表单设计

### 7.1 老师认证（飞书 OAuth 2.0）

**设计目标**：15 个辅导老师用飞书账号登录，无需维护独立账号系统。

**登录流程**：

```
老师访问系统 → 未登录时跳转 /login
    ↓
点击"飞书登录" → GET /api/auth/login → 302 到飞书 OAuth 授权页
    ↓
老师在飞书页面授权 → 飞书回调 /api/auth/callback?code=xxx
    ↓
后端用 code 换 access_token → 获取用户 open_id 和姓名
    ↓
在 teachers 表按 feishu_open_id 查找匹配（白名单校验）
    ↓
匹配成功 → 签发 JWT（httpOnly Cookie，7 天有效期）→ 302 跳转到工作台
    ↓
未在白名单 → 返回"无权访问"页面
```

**API**：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/auth/login` | GET | 302 重定向到飞书 OAuth 授权页 |
| `/api/auth/callback` | GET | code 换 access_token → 白名单校验 → 写 JWT Cookie |
| `/api/auth/me` | GET | 返回当前登录老师信息 |
| `/api/auth/logout` | POST | 清除 Cookie |

**配置**（`server/.env`）：
```
FEISHU_APP_ID=xxx
FEISHU_APP_SECRET=xxx
JWT_SECRET=xxx（随机 32 字节）
```

**前端**：
- 新增 `src/pages/login.vue`：单按钮"飞书登录"
- 路由守卫：`/api/auth/me` 失败时重定向到 `/login`
- 顶部导航栏：老师下拉选择 → 当前老师名 + 退出按钮

### 7.2 收作业表单

**设计目标**：家长通过专属链接提交作业图片，无需账号，最轻量。

**URL**：`/homework/submit/:token`

**Token 生成**：HMAC-SHA256 签名，编码 `studentId:teacherId:term`，签名密钥存 `.env`（`TOKEN_SECRET`）。老师在任务看板的学生卡片上一键复制专属链接。

**家长操作流程**：
1. 点击微信群/私聊置顶链接 → 打开系统收作业页面
2. 页面显示学生姓名（token 解析），引导上传今天的作业图片
3. 上传后自动调用视觉 API 识别学期/天数（可手动修改）
4. 点击"提交" → 进入批改队列 → 页面显示"已收到，老师正在批改"

**API**：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/submit/config/:token` | GET | 解析 token，返回学生姓名等信息（公开） |
| `/api/submit/:token` | POST | 接收图片，接入批改队列（公开） |
| `/api/dashboard/submit-links` | GET | 老师获取自己负责学生的专属链接（需登录） |

### 7.3 入营问卷（学生档案）

**设计目标**：辅导老师第一次接触家长时，生成入营链接发给家长填写，系统自动建档。

**URL**：`/homework/enroll/:token`

**Token 生成**：编码 `teacherId:term`，老师为该批次所有学生生成同一个入营链接。

**家长填写字段**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 孩子姓名 | 文本 | 是 | 用于建立 student 记录 |
| 年级/学期 | 选择 | 是 | 一上~六下，决定 enrollment.semester |
| 孩子当前数学水平 | 单选 | 否 | 优秀/良好/一般/较弱（self_level） |
| 薄弱知识点 | 文本 | 否 | 家长自填（weak_points） |
| 家长最希望重点关注的方面 | 文本 | 否 | 家长自填（parent_concern） |
| 家长姓名 | 文本 | 否 | parent_name |

**提交后**：
- 自动创建/查找 `students` 记录（按姓名去重）
- 自动创建 `enrollments` 记录（关联老师、期、学期）
- 创建 `student_profiles` 记录存储基线信息
- 页面显示"建档成功，欢迎加入计算营！"

**新增 `student_profiles` 表**：

```sql
CREATE TABLE student_profiles (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  student_id     TEXT NOT NULL REFERENCES students(id),
  term           TEXT NOT NULL,
  parent_name    TEXT,
  self_level     TEXT,    -- 优秀/良好/一般/较弱
  weak_points    TEXT,
  parent_concern TEXT,
  extra_notes    TEXT,
  created_at     TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(student_id, term)
);
```

**API**：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/enroll/config/:token` | GET | 解析 token，返回老师和期信息（公开） |
| `/api/enroll` | POST | 提交问卷，建立学生档案（公开） |
| `/api/dashboard/enroll-link` | GET | 老师生成当期入营链接（需登录） |
| `/api/students/:id/profile` | GET | 查看某学生的档案（需登录） |

---

## 八、后续规划与优先级

### P1：近期推进（天维度服务完善）

| 事项 | 对应服务 # | 承载平台 | 说明 |
|------|-----------|---------|------|
| 飞书 OAuth 登录 | — | 批改系统 | 15 个辅导老师用飞书账号登录，JWT Cookie 鉴权 |
| 系统内收作业表单 | — | 批改系统 | 家长通过 token 链接提交作业，替换飞书表单 |
| 入营问卷与学生建档 | — | 批改系统 | 家长填写基线信息，自动写入 student_profiles |
| 批改结果图导出 | #1 | 批改系统 | 一键导出带标注的批改图，作为家长"第一交付" |
| 当日全班汇总 | #4 | 批改系统 | 系统内生成视觉图片，老师导出发学期群 |
| 周日综合训练差异化 | #1 | 批改系统 | 周日综合训练与日常作业在反馈内容上做差异化设计 |

### P2：中期完善（周维度服务）

| 事项 | 对应服务 # | 承载平台 | 说明 |
|------|-----------|---------|------|
| 周错题汇总 | #9 | 批改系统 | 系统内生成 PDF，老师导出发微信 |
| 主讲教研数据 | #11 | 批改系统 | 学情分析页生成数据报告，给主讲备课 |
| 周日综合测试全班汇总 | #10 | 批改系统 | 系统内生成视觉图片，老师导出发学期群 |
| 加练题精准推荐 | #2 | 批改系统 | 基于错因画像而非单纯知识点匹配 |
| 学情侧滑面板 | — | 批改系统 | 老师在批改时可随时查看该学生的累积学情 |

### P3：远期规划（21 天维度）

| 事项 | 对应服务 # | 承载平台 | 说明 |
|------|-----------|---------|------|
| 个人学情复盘 | #12 | 批改系统 | 系统内生成视频，老师导出发微信。目标续费转化 |
| 全班学情复盘 | #13 | 批改系统 | 系统内生成视频/长图，老师导出发学期群 |
| 分班测试录入 | — | 批改系统 | 测试结果结构化录入，完善学生基线档案 |

---

## 九、与现有资产的关系

### 丁老师 Aily Skill（math-homework-feedback）

丁老师已有的飞书 Aily Skill 提供了：解题思路讲解、同类型加练题生成、个性化反馈话术（含全对/有错两种场景）、年级判断标准、错因分类框架。

本系统的 Tab 2（反馈建议）在设计上参考了该 Skill 的结构，并在以下方面做了增强：

| 维度 | Aily Skill | 本系统 |
|------|-----------|--------|
| 输入 | 需要人工整理题目+答案的文本 | 直接从图片批改结果获取，无需人工整理 |
| 身份 | 无学生绑定，每次独立 | 绑定学生档案，可引用历史数据 |
| 错因 | 通用分类 | 基于 API 返回的具体分析 + 大模型归类 |
| 加练 | 随机同类型题 | 三难度层级全量输出，老师可勾选导出 |
| 话术 | 通用模板 | 结合学生特点和历史表现定制 |
| 二次调整 | 无 | 支持直接编辑和 AI 重新生成 |

### V5 事件触发任务（历史）

此前的 V5 版本使用 Vision 大模型直接看图批改，无结构化数据输出，事件触发链路依赖飞书表单 → 多维表格 → OpenClaw。本系统已将整条链路迁移到系统内：批改引擎改用腾讯云专用 API，作业提交改为系统内 token 表单，不再依赖飞书多维表格。

### 收拢阶段解决记录

作业提交链路经历了从"飞书表单 + 多维表格 + Flow A/B"到"系统内 token 表单直接提交"的演进，详细背景见历史讨论记录。

---

## 十、更新记录

按时间顺序记录每次功能变更，最新的在最下面。

### 2026-03-22 · 初版

- 系统定位、服务周期分层、每日作业页面详细设计、数据流转、技术现状、后续规划

### 2026-03-23 · 反馈建议上线 + OpenClaw 对接

- 反馈建议模块：接入豆包大模型（doubao-seed-2-0-lite），模块化卡片 + KaTeX 公式渲染
- 三档反馈策略设计：全对/少量错题/较多错题，不同模块组合
- 加练推荐定位为最高价值模块
- 自动批改链路：新增 submit-url API + 结果持久化 + URL 直达
- 与 OpenClaw 对接：事件触发 → 批改 → 写回多维表格，系统解耦设计
- 学生元数据：submit-url 接口支持传入学生名字/学期/第几天

### 2026-03-25 · 部署上线 + 图片优化

- 部署到生产服务器（PM2 + Nginx/OpenResty）
- 端到端链路验证通过
- 图片分辨率缩放（最大长边 1600px）解决上传超时问题
- Nginx 缓存控制（HTML no-cache）解决前端缓存问题

### 2026-03-26 · 老师反馈第一轮优化

基于辅导老师实际使用反馈，集中处理以下问题：

- **禁词机制**：prompt 中增加详细的禁词规则（笼统归因、否定能力、主观态度、贬低错误、负面评判五类），以及表述规则和错因分析标准示范
- **加练顺序调整**：基础巩固 → 巩固加练 → 挑战加练（由易到难）
- **批改复核筛选栏**：新增置顶筛选栏（全部/错题/正确/已展示/已隐藏 + 数量）
- **正确/错误切换**：从单按钮改为双按钮组（✓ 正确 / ✗ 错误），更醒目
- **题目隐藏/展示**：每道题卡片上增加隐藏/展示切换，隐藏后视觉弱化不消失，联动左侧标注
- **天数导航 bug 修复**：切换天数时先加载已有反馈，防止重复触发生成
- **KaTeX 渲染增强**：更严格的公式验证、LLM 输出清洗（sanitizeModelOutput）、渲染失败优雅降级（safeKatex）
- **复制按钮 HTTP 降级**：navigator.clipboard 不可用时自动降级为 textarea + execCommand
- **jobId 隔离**：切换 job 时 AbortController 中止旧请求，防止反馈数据混淆
- **批改复核答案详情**：手写答案、正确答案、分析字段支持 KaTeX 公式渲染

### 2026-03-27 · 大图查看 + 状态标记 + 学期切换

- **大图查看模式**：全屏浮层，图片撑满容器，标注精确覆盖；支持逐个/全部隐藏标注
- **主视图标注控制**：作业原图区域增加全部显示/全部隐藏按钮
- **核对/反馈状态标记**：Tab 旁增加"未核对/已核对""未反馈/已反馈"按钮，持久化到后端
- **天数导航状态小点**：每天 tab 下显示核对/反馈状态小点，多次提交显示多行
- **学期切换**：同一学生多个学期数据时，支持切换学期查看（后端按学期分组返回）
- **修改痕迹识别**：prompt 指导模型观察图片中的涂改痕迹，在评价和错题分析中体现

### 2026-03-28 · 加练拼盘 + 统一四模块 + 模块编辑

- **加练拼盘**：三个难度层级合并为一个"加练题目"模块，支持单题勾选、按难度全选/取消
- **加练导出**：选中题目导出为图片（复制到剪贴板或下载 PNG），只含题目不含答案
- **统一四模块结构**：去掉三档 tier 分支（全对/少量错题/较多错题），固定为"整体评价 → 学情分析 → 加练题目 → 家长反馈话术"四个模块，由模型根据实际数据个性化生成
- **模块二改为"学情分析"**：全对时做掌握确认，有错题时逐题分析+纠正建议，不管什么情况都输出
- **模块级直接编辑**：每个模块卡片支持点击"编辑"后直接修改 Markdown 原文，保存后重新渲染并持久化
- **模块级 AI 重新生成**：每个模块支持输入补充指令后单独调用 AI 重新生成，其他模块不受影响
- **后端新增接口**：`POST /api/feedback/regenerate-module` 单模块重新生成（流式）

### 2026-03-29 · 模块顺序调整 + 推理加速 + Bug 修复

- **模块顺序调整**：四模块顺序从"整体评价 → 学情分析 → 加练题目 → 家长反馈话术"调整为"一、整体评价 → 二、作业分析 → 三、家长反馈话术 → 四、加练题目"，加练放到最后更符合阅读习惯
- **模块二改名为"作业分析"**：从"学情分析"改为"作业分析"，更准确地表达该模块是对本次作业的具体分析，而非对学生整体学情的评估
- **加练题目标题规范**：加练模块卡片标题从"加练题目"改为"四、加练题目"，与其他三个模块的编号格式保持一致
- **加练题目对齐修复**：修复答案区域部分居中、部分左对齐的问题。原因是 MarkdownKatex 组件在 `inline` 模式下仍将独占一行的公式渲染为块级（`display: true`），触发 `.katex-display` 的居中样式。修复为 `inline` 模式下强制所有公式为行内渲染
- **推荐标记增强**：加练子标题的推荐标记正则从仅匹配 `**基础巩固加练【推荐】**` 扩展为同时支持推荐在加粗外部、有空格、圆括号等多种 LLM 输出变体；prompt 中增加格式示例引导模型稳定输出
- **反馈生成加速**：为反馈生成（`/api/feedback/generate`）和单模块重新生成（`/api/feedback/regenerate-module`）两个接口添加 `reasoning_effort: 'minimal'`，关闭豆包大模型的深度思考，显著减少首字输出延迟
- **模块重新生成 Bug 修复**：修复单模块重新生成请求 URL 双重 `/api` 前缀导致 404 的问题（`/api/api/feedback/regenerate-module` → `/api/feedback/regenerate-module`）

### 2026-03-29 · 飞书多维表格直连集成

将作业提交链路从"多维表格 → 群消息 → OpenClaw → 批改系统 → OpenClaw → 多维表格"的多跳架构，简化为"多维表格 → 批改系统 → 多维表格"的直连架构：

- **新增异步批改接口**：`POST /api/correction/submit-async`，接收 `imageUrl`、`studentName`、`semester`、`day`、`callbackUrl`，立即返回 `{ jobId, status, link }`，后台异步完成批改后回调飞书工作流 Webhook
- **Flow A（飞书自动化 → 批改系统）**：多维表格新增记录时自动触发，第 1 步获取记录字段值，第 2 步发送 HTTP POST 请求到 `/api/correction/submit-async`，第 3 步将返回的 `jobId` 和"处理中"状态写回当前记录
- **Flow B（批改系统 → 飞书工作流）**：批改完成后，系统 POST 回调飞书工作流 Webhook（`{ jobId, status, link }`），工作流按 `jobId` 查找记录并更新批改状态和作业批改链接
- **多维表格新增 jobId 列**：用于 Flow A 写入和 Flow B 查找匹配，作为两个流程的关联键
- **去除 OpenClaw 中间依赖**：整条链路不再经过飞书群消息和 OpenClaw，减少故障点，降低延迟

### 2026-03-29 · 天数扩展 + 学期简写 + 排序优化

- **天数扩展至 24 天**：天数导航从 1-21 扩展为 1-24，其中 22/23/24 分别对应第一/二/三周易错点加练
- **视觉识别特殊映射**：识别 prompt 新增规则，"第X周易错点加练"自动映射为第 22/23/24 天
- **学期简写**：学期选项从"二年级上"简化为"二上"，所有提交接口入口处统一标准化；视觉识别 prompt 和返回值也同步简写
- **已有数据自动清洗**：后端读取 job 数据时，自动将旧格式（如"二年级下"）转为简写（"二下"）并回写到文件，无需手动迁移
- **学期排序**：学期列表按年级（一→六）+ 上下学期自动排序，确保"二上"在"二下"前，"二下"在"四下"前

### 2026-03-31 · 复核状态持久化 + 人工标注 + 加练重新生成 + UI 优化

- **复核状态持久化**：老师修改的对错判定（reviewCorrections）和隐藏的答案（hiddenAnswerIds）自动保存到后端 job JSON，刷新页面后恢复老师最终判定结果，不再丢失。新增 `POST /api/jobs/:jobId/review-state` 接口，前端修改时 500ms 防抖自动保存
- **人工标注功能**：老师可切换到"人工标注"模式，在作业图片上框选 AI 未识别到的题目并标记对/错。人工标注与系统标注视觉外观完全一致（家长截图无法区分），计入总题数和正确率统计。点击已有人工标注弹出菜单（正确/错误/删除）。人工标注模式下 AI 标注降低透明度以避免干扰。标注数据持久化到后端
- **加练模块支持重新生成**：加练题目模块（四、加练题目）与一、二、三模块一样支持输入补充指令后 AI 单独重新生成
- **加练取消选中样式优化**：取消选中的题目仅内容区置灰，选择框保持清晰可见
- **隐藏状态视觉优化**：隐藏的答案仅内容区降低透明度，「已隐藏」按钮保持正常显示，确保操作控件始终可见
- **Tab 栏视觉改版**：批改复核 / 反馈建议 Tab 从底部下划线样式改为卡片式大块按钮（圆角边框 + 填充背景 + 选中高亮），视觉上呈现为两个独立区块

### 2026-04-01 ~ 04-08 · 数据底座 + 工作台 + 学情分析 + 周报

- **SQLite 数据底座上线**：新增 teachers / students / enrollments / jobs / answers / reports / teacher_preferences 七张表，从现有 job JSON 提取结构化字段双写入 SQLite，支持多维度交叉查询
- **任务看板（工作台首页）**：三大区域（任务总览 / 待办任务 / 班课学生看板），学生×24天状态矩阵，文件夹式 Tab + 卡片网格 + 冻结/提醒/排序偏好持久化
- **学情分析**：个人学情（正确率趋势 + 知识点掌握 + 错题列表）+ 全班概览（6 模块）+ AI 教研建议
- **周学习报告**：A4 分页设计（基本信息/精彩表现/易错汇总/学习建议/老师备注），大拇指评价，LLM 流式生成 + PDF 导出
- **学期总结视频**：Remotion + FFmpeg + 火山引擎 TTS，6 个场景 + 逐句字幕 + BGM
- **全局导航**：顶部导航栏 + 老师身份选择 + 期选择器 + 学期筛选
- **反馈建议升级为六模块**：整体情况反馈 / 书写评价 / 问题分析 / 整体改进计划 / 给家长的小提示 / 加练题目
- **飞书直连集成**：Flow A（自动化）直接调用 submit-async API + Flow B（工作流）接收回调写回表格

### 2026-04-09 ~ 04-12 · 数据清洗 + 字段校验 + 性能优化

- **term 和 teacher_id 数据补全**：jobs 表新增 teacher_id 字段；编写 `migrate-term-teacher.ts` 清洗脚本，按 jobId 分界线将历史数据分为 3月/4月期，teacher_id 统一设为欣欣老师；同步清洗 225 个 JSON 文件
- **飞书工作流传参**：Flow A 新增传递 `term`（班课月份）和 `teacherName`（负责老师）到 submit-async 接口
- **字段校验**：semester 仅接受一上~六下格式，day 仅接受纯数字，非标准值视为空
- **反馈生成加速**：新增 `loadImageBase64Compressed()` 函数，用 sharp 将原图压缩至长边 1200px / JPEG quality 70 后再送入豆包多模态输入；fetch 加 `AbortSignal.timeout(600_000)` 兜底
- **测试数据清理**：删除段喆、王欣、王欣1、王欣2、果果、王一涵等测试 enrollment 记录
- **学期排序修复**：新增 `semesterWeight()` 函数，学期按一上→一下→二上→……→六下自然排序

### 2026-04-13 ~ 04-15 · 服务天数 + 看板增强 + 周报修复

- **期起始日期与服务天数**：顶部导航期选择器旁新增日期设置入口（date picker），后端 `GET/PUT /api/dashboard/term-start` 存取；Dashboard 问候语旁显示 VIP 金色标签「服务期 第X/24天」
- **未提交统计修正**：缺交天数只统计到当前服务天数，不再把未来天数算作缺交；提醒文案生成时传入 `currentDay` 让 LLM 感知进度
- **任务看板天数固定为 24**：maxDay 从动态计算改为固定 24，保证矩阵始终完整
- **昨日回顾 UI 优化**：换行展示，语气更贴近日常
- **周报 reasoning_effort 调优**：从 `'minimal'` 调整为 `'low'`，输出质量显著改善
- **周报思考内容过滤**：后端过滤 `delta.reasoning_content`，前端在收到实际内容后才切换 loading 状态，防止 LLM 思考过程泄漏到页面
- **PDF 导出修复**：安装 Puppeteer 系统依赖（libatk 等）+ Emoji 字体（fonts-noto-color-emoji）

### 2026-04-19 · LaTeX 渲染修复 + 部署流程优化

- **`\cdots` 渲染修复**：OCR 产出的 `\cdots\cdots` 在 KaTeX 中渲染为乱码 `⋅s⋅s`，统一替换为 `\ldots`
- **`\enclose{horizontalstrike}` 剥离**：KaTeX 不支持 `\enclose`，OCR 竖式公式因此整体 fallback 为原始文本；新增剥离规则使 array 竖式正常渲染
- **`\\hline` 行换符修复**：双反斜杠清洗规则误将 `\\hline` 缩减为 `\hline`，丢失 array 行换符导致 KaTeX 报错；预处理补空格 + regex 负向前瞻修复
- **前端部署流程修正**：本地 `npm run build` 产出的 `dist/` 需通过 `rsync` 上传服务器后再部署，不能依赖 `git pull`（`dist/` 不在版本控制中）

### 2026-04-30 · 飞书 OAuth 登录 + 系统内表单 + 去掉多维表格依赖

- **飞书 OAuth 2.0 登录**：15 个辅导老师用飞书账号登录；JWT Cookie（httpOnly，7 天有效期）；路由守卫保护所有老师页面；顶部导航老师身份改为登录态显示
- **系统内收作业表单**（`/submit/:token`）：家长通过 HMAC-SHA256 签名专属链接提交作业图片，无需任何账号；视觉 API 自动识别学期/天数；接入现有批改队列
- **入营问卷**（`/enroll/:token`）：老师发送链接，家长填写学生基线信息（水平/薄弱点/关注点），自动建档写入 student_profiles 表
- **新增 `student_profiles` 表**：存储入营基线信息（parent_name / self_level / weak_points / parent_concern）
- **teachers 表新增 `feishu_open_id` 字段**：用于 OAuth 回调时的白名单校验
- **去掉飞书多维表格依赖**：提交入口、数据存储、状态管理全部迁移到系统内；原飞书 Flow A/B 自动化链路停用；/api/correction/submit-async 接口保留但标注为兼容接口

### 2026-05-01 · 班主任工作台 + 通用收作业 + 多老师多营支持

- **系统品牌升级**：左上角名称从“计算营 AI Copilot”调整为“营课管理系统”，为后续应用题营、英语营、语文营等多营扩展预留统一入口
- **多老师数据隔离**：辅导老师登录后默认只看到自己负责的学生、作业、学情和周报数据；管理员/主讲角色可按权限查看多老师数据
- **多营与多期切换**：新增 camp / cohort 数据模型，支持计算营、应用题营等不同营种切换；顶部导航增加营和期选择器，页面数据随当前营期联动
- **班主任工作台上线**：新增“班主任工作台”，集中承载学生管理、今日任务、家长链接、老师管理和代交作业等班主任日常动作
- **通用收作业链接**：取消每个学生一个专属链接的日常分发方式，改为班级统一收作业入口；家长首次通过学生姓名和手机号完成绑定，后续可直接提交作业
- **家长手机号校验**：入营问卷和班主任手动添加学生都要求填写家长手机号；系统仅保存手机号 hash，用于通用收作业链接的首次绑定校验
- **老师代交作业迁移**：将老师端“代交作业”从作业批改页移到班主任工作台学生列表中，作业批改页回归为纯批改、复核和反馈页面
- **工作台看板口径调整**：顶部概览只展示班级状态（服务学生、今日已提交、今日未提交、今日提交率）；待批改、待反馈等行动项统一放入“今日任务”，减少重复信息
- **公开表单体验修复**：修复入营问卷、通用收作业页在预览环境白屏的问题；优化移动端拍照上传方式，改用原生文件输入覆盖上传区域，提升手机端可用性
