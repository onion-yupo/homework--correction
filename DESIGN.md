# Design System — 洋葱学园学情报告

## 1. Visual Theme & Atmosphere

洋葱学园的学情报告体系呈现出一种**教育科技感与亲和力并存**的视觉语言。整体设计面向小学至高中阶段的学生家长，需要在数据密度与可读性之间取得平衡：大量数字指标被组织在白色卡片内，以 3×2 网格排布，配合趋势箭头和颜色编码，让家长"一眼就能看到进步或退步"。

色彩基调以**洋葱蓝**（`#4A90E2`）为锚点，从顶部的蓝色渐变 Banner（叠加半透明三角形几何纹理）一路延伸到章节标题左侧的蓝色竖条、图表中的蓝色系列、以及蓝色描边的鼓励语气泡。白色（`#FFFFFF`）卡片浮在浅灰（`#F5F7FA`）底色之上，形成干净、轻量的信息层次。

趋势指示系统是视觉语言的核心之一：绿色向上箭头 `↑` 表达进步、红色向下箭头 `↓` 表达退步、橙色横杠 `-` 表达持平。这三个符号贯穿所有 KPI 数据模块，是家长第一眼就会捕捉的信息。

设计中融入了**游戏化元素**：学生档案使用科幻感的切角蓝色描边卡片，配合二次元角色头像和"星球身份：高级研究员"等游戏化称号，将学习数据包装为一份"研究员档案"。

**关键特征：**
- 移动端优先（375px 视口），单列卡片流布局
- 洋葱蓝（`#4A90E2`）作为唯一品牌色贯穿全局
- 蓝色渐变 Banner + 半透明三角形几何纹理作为页面头部
- 白色卡片 + 浅灰底色 + 极浅阴影的分层体系
- 三色趋势指示：绿色 `↑` / 红色 `↓` / 橙色 `-`
- 章节标题统一使用"蓝色竖条 `|` + 粗体标题 + 关键数字蓝色高亮"格式
- 鼓励性文案置于蓝色圆角描边气泡中，语气亲切（带 `~` `！` `哦` 等语气词）
- 黄色圆角胶囊按钮作为行动号召（"去巩固"、"去学完"、"分享给朋友"）
- 图表使用金黄 vs 蓝色的双色对比（"我的" vs "平均"）
- 多色学科色板用于饼图和分类标识

## 2. Color Palette & Roles

### Primary Brand
- **洋葱蓝** (`#4A90E2`): 品牌主色。用于章节标题竖条、描边框、图表"平均"系列、环形图、Tab 选中态、链接文字
- **渐变蓝起始** (`#5BA0F0`): Banner 渐变左侧/上方起点
- **渐变蓝终止** (`#7AB8FF`): Banner 渐变右侧/下方终点
- **深蓝** (`#3A7BD5`): 全宽蓝色按钮背景（"英语学情分析"等导航 CTA）

### Background & Surface
- **页面底色** (`#F5F7FA`): 全局浅灰背景，让白色卡片浮起
- **卡片白** (`#FFFFFF`): 所有内容卡片的背景色
- **Banner 蓝** (linear-gradient `#5BA0F0` → `#7AB8FF`): 页面头部渐变区域

### Text
- **主文字** (`#333333`): 标题、数据数字、正文
- **副文字** (`#666666`): 卡片内描述文字、图表标签
- **灰色标签** (`#999999`): KPI 指标下方的标签（"学习时长"、"答题数"等）
- **浅灰占位** (`#CCCCCC`): Tab 未选中文字、禁用态

### Trend Indicators
- **趋势上升绿** (`#52C41A`): 向上箭头 `↑`，表达正向趋势
- **趋势下降红** (`#FF4D4F`): 向下箭头 `↓`，表达负向趋势
- **趋势持平橙** (`#FFA500`): 横杠 `-`，表达无明显变化

### Chart — Dual Series
- **"我的" 金黄** (`#F5C542`): 柱状图/折线图中代表学生个人数据
- **"平均" 蓝** (`#4A90E2`): 柱状图/折线图中代表洋葱平均/同年级平均

### Chart — Donut
- **答题正确率环** (`#F5C542`): 金黄色，用于答题正确率的环形图
- **改错正确率环** (`#4A90E2`): 蓝色，用于改错正确率的环形图
- **完成率环** (`#4A90E2`): 蓝色，用于知识点完成率的环形图
- **环形底色** (`#F0F0F0`): 环形图未填充部分

### Action CTA
- **黄色 CTA** (`#FFD426`): "分享给朋友"、"去巩固"、"去学完" 等行动按钮背景
- **黄色 CTA 文字** (`#333333`): 黄色按钮上的深色文字
- **蓝色 CTA** (`#4A90E2`): 全宽导航按钮（"英语学情分析"）
- **蓝色 CTA 文字** (`#FFFFFF`): 蓝色按钮上的白色文字

### Decorative
- **蓝色描边框** (`#4A90E2` at 30% opacity): 鼓励文字气泡、高亮框的圆角描边
- **蓝色虚线** (`#4A90E2` at 40% opacity): 章节间的虚线分隔符
- **科技感边框** (`#7AB8FF`): 学生档案卡的切角描边

### Subject Color Palette

#### 小学
| 学科 | 色值 |
|------|------|
| 数学 | `#7ED7FF` |
| 地球 | `#A9EBD0` |
| 实验 | `#FFEB84` |
| 语文 | `#FFADAD` |
| 自然 | `#A3ED98` |

#### 初中
| 学科 | 色值 |
|------|------|
| 数学 | `#1FC3FF` |
| 语文 | `#FF707A` |
| 英语 | `#FEA345` |
| 生物 | `#52CC5E` |
| 化学 | `#8470FF` |
| 物理 | `#FFD633` |
| 地理 | `#4FC6A9` |

#### 高中
| 学科 | 色值 |
|------|------|
| 数学 | `#34B1FF` |
| 语文 | `#FA5A65` |
| 英语 | `#26B638` |
| 生物 | `#26B638` |
| 化学 | `#604CE4` |
| 物理 | `#89A509` |
| 地理 | `#E29E39` |

#### 中职
| 学科 | 色值 |
|------|------|
| 数学 | `#4096FF` |
| 英语 | `#FF7B29` |

### Tooltip & Overlay
- **Tooltip 背景** (`#333333` at 90% opacity): 图表悬浮提示的深色背景
- **Tooltip 文字** (`#FFFFFF`): 提示框内白色文字
- **图表高亮带** (`#4A90E2` at 10% opacity): 折线图当前周的浅蓝色竖向高亮区域

## 3. Typography Rules

### Font Family
- **Primary**: `PingFang SC`, with fallbacks: `-apple-system, BlinkMacSystemFont, "Helvetica Neue", "Hiragino Sans GB", "Microsoft YaHei", Arial, sans-serif`
- **Numeric Display**: `DIN Alternate` or system default bold — 数字使用更紧凑的字形以增强数据感

### Hierarchy

| 角色 | 字号 | 字重 | 行高 | 场景 |
|------|------|------|------|------|
| Banner 标题 | 20px (1.25rem) | 700 | 1.40 | "学情报告"、"学习情报局" 页面标题 |
| 章节标题（外） | 18px (1.13rem) | 700 | 1.50 | 蓝色竖条旁的章节标题，关键数字蓝色高亮 |
| 卡片标题 | 18-20px (1.13-1.25rem) | 700 | 1.40 | 卡片内的小标题（"本周答题情况"、"答题正确率变化趋势"） |
| KPI 数字 | 32-40px (2.00-2.50rem) | 700 | 1.00 | 核心指标数字（"92"、"1200"、"85"） |
| KPI 单位 | 14-16px (0.88-1.00rem) | 400 | 1.00 | 紧跟数字的单位文字（"分钟"、"个"、"道"、"%"） |
| 环形图百分比 | 28-32px (1.75-2.00rem) | 700 | 1.00 | 环形图中心的百分比数字 |
| 图表数值标签 | 12-14px (0.75-0.88rem) | 500 | 1.00 | 柱状图顶部、折线图数据点旁的数值 |
| 正文 | 14-16px (0.88-1.00rem) | 400 | 1.60 | 描述性文字、鼓励语、知识点名称 |
| 标签 | 12-14px (0.75-0.88rem) | 400 | 1.50 | KPI 下方标签（"学习时长"）、图例文字、图表轴标签 |
| Tab 文字 | 16px (1.00rem) | 500 (active) / 400 (inactive) | 1.50 | Tab 切换项 |
| 档案信息 | 16px (1.00rem) | 400 | 1.80 | 学生档案卡中的键值对（"姓名：清华皮皮"） |
| 日期副标题 | 14px (0.88rem) | 400 | 1.50 | "223.8.28-23.09.01" 日期范围 |

### Principles
- **数字即主角**：KPI 数字使用 32-40px 粗体，是页面最大最醒目的元素。紧跟的单位文字（"分钟"、"个"）使用正常字重、小一号的尺寸，避免与数字抢注意力
- **趋势箭头紧随数字**：`↑` `↓` `-` 符号紧贴在数字+单位后方，使用对应的趋势颜色（绿/红/橙），与数字形成视觉组合
- **标签弱化**：KPI 下方的灰色标签（如"学习时长"）使用 12-14px #999，刻意低调，让用户先看数字再看标签
- **章节标题格式统一**：`蓝色竖条(4px×20px)` + `空格` + `粗体文字` + `关键数字(蓝色高亮)`，例如 `| 本周学习 4 个知识点，刷了 34 道题`

## 4. Component Stylings

### KPI Stat Grid
- **布局**: 3 列 × 2 行网格，`gap: 16px`
- **单元格**: 居中对齐
- **数字行**: `[数字(32-40px bold #333)] + [单位(14-16px regular #333)] + [趋势箭头(14px 对应颜色)]`
- **标签行**: `12-14px regular #999`，数字下方 4px
- **容器**: 白色卡片，`border-radius: 12px`，内边距 20px 16px

### Section Header（章节标题条）
- **竖条**: `width: 4px; height: 20px; background: #4A90E2; border-radius: 2px`
- **标题文字**: `18px bold #333`，距竖条左侧 8px
- **高亮数字**: 标题中的关键数字使用 `color: #4A90E2; font-weight: 700`
- **示例**: `|` 本周学习 **4** 个知识点，刷了 **34** 道题

### Donut Chart（环形图）
- **尺寸**: 直径 80-100px
- **环宽**: 10-12px
- **填充色**: 金黄 `#F5C542`（答题正确率）/ 蓝 `#4A90E2`（改错正确率、完成率）
- **底环色**: `#F0F0F0`
- **中心数字**: `28-32px bold #333`
- **中心单位**: `%` 紧跟数字，`16px regular`
- **底部标签**: `12px regular #999`，环形图正下方

### Bar Chart（双色柱状图）
- **"我的" 柱**: `#F5C542` 金黄，`border-radius: 4px 4px 0 0`
- **"平均" 柱**: `#4A90E2` 蓝色，`border-radius: 4px 4px 0 0`
- **柱宽**: 24-32px，同组两柱间距 4px
- **数值标签**: 柱顶上方 4px，`12-14px #333`
- **X 轴标签**: `14px #666`，底部
- **图例**: 圆点 `●` 8px + 文字 `14px #666`，左上角

### Line Chart（双色折线图）
- **"我的" 曲线**: `#F5C542` 金黄，`stroke-width: 2.5px`，平滑贝塞尔曲线
- **"平均" 曲线**: `#4A90E2` 蓝色，`stroke-width: 2.5px`，平滑贝塞尔曲线
- **当前周高亮**: 竖向半透明蓝色带 `background: rgba(74, 144, 226, 0.1)`
- **Tooltip**: 深色圆角框 `background: rgba(51,51,51,0.9); border-radius: 8px; padding: 8px 12px`
- **X 轴**: 日期范围文字 `12px #999`
- **底部气泡**: 蓝色描边圆角框内的鼓励语

### Pie / Donut Chart（多色饼图）
- **颜色**: 取自学科色板
- **中心数字**: `28-32px bold #333` + `14px regular #999` 标签
- **外部标注**: 引导线 + 学科名 + 百分比 + 时长
- **Tooltip**: 深色背景，列出各学科占比和时长

### Encouragement Bubble（鼓励语气泡）
- **背景**: `#FFFFFF`
- **边框**: `1px solid rgba(74, 144, 226, 0.3)`
- **圆角**: `12px`
- **内边距**: `16px 20px`
- **文字**: `14-15px regular #333`，语气亲切
- **布局**: 居中，卡片底部

### Action Button — Yellow Pill（黄色胶囊按钮）
- **背景**: `#FFD426`
- **文字**: `16px bold #333`
- **圆角**: `24px`（全圆角胶囊形）
- **内边距**: `10px 24px`
- **用途**: "去巩固"、"去学完"、"分享给朋友"
- **全宽变体**: `width: 100%; padding: 14px 0`（"分享给朋友"）

### Action Button — Blue Full（蓝色全宽按钮）
- **背景**: `#4A90E2`
- **文字**: `16px bold #FFFFFF`
- **圆角**: `12px`
- **内边距**: `14px 0`
- **宽度**: `100%`
- **右侧图标**: 白色方框箭头 `[>>]`
- **用途**: "英语学情分析" 等模块导航

### Text Link with Arrow（文字链接）
- **文字**: `14px medium #4A90E2`
- **箭头**: `[>>]` 蓝色，紧跟文字
- **用途**: "去改错"、"看看你本周的学习成果"

### Tab Navigation（Tab 切换）
- **Active**: `16px medium #4A90E2`，底部 `2px solid #4A90E2`
- **Inactive**: `16px regular #CCCCCC`，无底线
- **间距**: Tab 项之间 24px

### Profile Card（研究员档案卡）
- **整体**: 白色背景，科幻风格蓝色切角描边
- **边框**: `2px solid #7AB8FF`，四角有切角装饰（45° 斜切 + 加粗蓝色描边）
- **头像**: 右侧二次元角色插图，约 120×160px
- **信息表**: 左侧键值对布局，键（"姓名"、"星球身份"）灰色，值深色粗体
- **底部指标栏**: 4 列等宽网格，数字粗体 + 单位 + 灰色标签

### Knowledge Point List（知识点列表）
- **容器**: 蓝色虚线描边 `1px dashed rgba(74, 144, 226, 0.4); border-radius: 12px`
- **顶部文字**: 鼓励语 `14px #333`
- **列表项**: 知识点名称（左）+ 黄色胶囊按钮"去学完"（右）
- **分隔**: 列表项之间 `1px solid #F0F0F0`
- **内边距**: `16px`

### Blue Gradient Banner（蓝色渐变头部）
- **背景**: `linear-gradient(135deg, #5BA0F0, #7AB8FF)`
- **纹理**: 半透明白色三角形几何图案 overlay（`opacity: 0.15`）
- **圆角**: `16px`（当作为卡片头时）
- **内容**: 白色文字标题 + 日期范围

### Dashed Separator（虚线分隔符）
- **样式**: `border-top: 2px dashed rgba(74, 144, 226, 0.3)`
- **用途**: 章节之间的视觉分隔

## 5. Layout Principles

### Spacing System
- **Base unit**: 8px
- **Scale**: 4px, 8px, 12px, 16px, 20px, 24px, 32px, 40px, 48px
- **卡片内边距**: 16-20px 水平，20-24px 垂直
- **卡片间距**: 24-32px 垂直间距
- **页面水平边距**: 16px 左右

### Grid
- **KPI 网格**: 3 列等宽，`gap: 16px`
- **档案底部指标**: 4 列等宽，`gap: 8px`
- **图表 + 指标**: 左侧指标（2 列）+ 右侧环形图（1 列），`gap: 16px`
- **知识点列表**: 单列，文字左 + 按钮右

### Container
- **最大宽度**: 375px（移动端），无桌面端扩展
- **单列流**: 所有内容垂直堆叠，无水平并排区块
- **卡片**: 全宽（减去 16px 左右 padding），`border-radius: 12-16px`

### Whitespace Philosophy
- **卡片即呼吸**：每个数据模块封装在独立的白色卡片中，卡片之间 24-32px 的垂直间距提供视觉呼吸
- **数字需要空间**：KPI 数字（32-40px）周围保持充足留白，避免数据拥挤
- **章节标题前空间更大**：章节标题前 32-40px 间距，后 16px 间距，建立清晰的层级分割
- **底部留白**：页面底部至少 40px 留白，避免内容紧贴屏幕边缘

### Border Radius Scale
| 级别 | 圆角 | 用途 |
|------|------|------|
| Micro | 4px | 柱状图顶部、小标签 |
| Standard | 8px | Tooltip、小型容器 |
| Card | 12px | 标准内容卡片、鼓励气泡、输入框 |
| Large | 16px | 大卡片、Banner、档案卡 |
| Pill | 24px | 黄色胶囊按钮 |
| Full | 9999px | 圆形图例标记 |

## 6. Depth & Elevation

| 层级 | 处理方式 | 用途 |
|------|----------|------|
| Level 0（底色） | `background: #F5F7FA`，无阴影 | 页面全局背景 |
| Level 1（卡片） | `background: #FFFFFF; box-shadow: 0 2px 8px rgba(0,0,0,0.06)` | 所有白色内容卡片 |
| Level 2（浮层） | `background: #FFFFFF; box-shadow: 0 4px 16px rgba(0,0,0,0.1)` | 弹窗、悬浮提示框 |
| Border Subtle | `1px solid rgba(74, 144, 226, 0.3)` | 鼓励语气泡、知识点列表容器 |
| Border Dashed | `2px dashed rgba(74, 144, 226, 0.3)` | 章节分隔线 |
| Border Sci-fi | `2px solid #7AB8FF` + 切角装饰 | 档案卡特殊边框 |
| Highlight Band | `background: rgba(74, 144, 226, 0.1)` | 折线图当前周高亮带 |

**阴影哲学**：设计整体非常扁平，阴影存在但极其克制——仅用于将白色卡片从浅灰底色中"托起"。不使用多层复合阴影或深色投影。视觉层次主要依靠**背景色差异**（浅灰底 → 白色卡片）和**蓝色描边**来建立，而非阴影深度。这种"轻阴影 + 描边"的组合让界面保持干净的教育产品气质。

## 7. Interaction & Motion

### Trend Indicators（趋势指示）
- **上升**: 绿色向上箭头 `↑`（`#52C41A`），紧跟在数字+单位后
- **下降**: 红色向下箭头 `↓`（`#FF4D4F`），紧跟在数字+单位后
- **持平**: 橙色横杠 `-`（`#FFA500`），紧跟在数字+单位后
- 箭头使用与数字相近的字号但稍小（约 14-16px），加粗

### Chart Interactions
- **Tooltip hover**: 图表数据点悬浮时显示深色圆角 Tooltip
- **折线图高亮带**: 当前周/选中周显示浅蓝色竖向高亮区域
- **柱状图高亮框**: 选中的学科对比柱外显示蓝色描边矩形

### Button States
- **Yellow Pill Default**: `background: #FFD426`
- **Yellow Pill Pressed**: `background: #E5BE22`（稍暗 10%）
- **Blue CTA Default**: `background: #4A90E2`
- **Blue CTA Pressed**: `background: #3A7BD5`（稍暗）

### Tab States
- **Active**: 蓝色文字 + 底部 2px 蓝色实线
- **Inactive**: 浅灰文字，无底线
- **切换**: 无动画，即时切换

### Transitions
- 最少动效，信息优先。图表可使用入场动画（环形图从 0 填充到目标值，约 600ms ease-out）
- 页面滚动流畅，无视差效果
- Tab 切换内容无过渡动画，即时替换

## 8. Responsive Behavior

### Breakpoints
| 名称 | 宽度 | 说明 |
|------|------|------|
| Mobile（主要） | 320-414px | 设计基准，所有截图均为此尺寸 |
| Tablet | 415-768px | 可考虑卡片左右 padding 增大，KPI 网格保持 3 列 |
| Desktop | >768px | 如需桌面展示，卡片最大宽度 480px 居中 |

### Touch Targets
- 黄色胶囊按钮：高度 44px+，满足移动端最小触控目标
- Tab 项：16px 文字 + 24px 间距，触控区域充足
- "去改错" 等文字链接：至少 44×44px 触控热区
- 图表 Tooltip 触发：整个数据点区域可触发，无需精确点击

### Collapsing Strategy
- 此设计为**纯移动端**，不存在桌面到移动的折叠需求
- 如需桌面适配：KPI 3×2 网格可扩展为 6×1 单行，或保持 3×2
- 柱状图/折线图保持原始布局，水平方向可扩展间距
- 饼图保持固定尺寸，周围标注可扩展为更宽的径向布局

## 9. Agent Prompt Guide

### Quick Color Reference
- 品牌蓝: `#4A90E2`
- Banner 渐变: `linear-gradient(135deg, #5BA0F0, #7AB8FF)`
- 页面背景: `#F5F7FA`
- 卡片背景: `#FFFFFF`
- 主文字: `#333333`
- 副文字: `#666666`
- 标签文字: `#999999`
- 趋势上升: `#52C41A`
- 趋势下降: `#FF4D4F`
- 趋势持平: `#FFA500`
- 图表"我的": `#F5C542`（金黄）
- 图表"平均": `#4A90E2`（蓝）
- 黄色 CTA: `#FFD426`
- 蓝色描边: `rgba(74, 144, 226, 0.3)`
- 卡片阴影: `0 2px 8px rgba(0,0,0,0.06)`
- 环形图底色: `#F0F0F0`

### Example Component Prompts
- "创建一个 KPI 数据网格：3列2行，白色卡片背景 `#FFFFFF`，`border-radius: 12px`，`box-shadow: 0 2px 8px rgba(0,0,0,0.06)`。每个单元格居中显示：数字 `32px PingFang SC bold #333`，单位 `14px regular #333` 紧跟数字，趋势箭头（绿色 `#52C41A` ↑ 或红色 `#FF4D4F` ↓ 或橙色 `#FFA500` -）紧跟单位。标签 `12px regular #999` 在数字下方 4px。"
- "创建章节标题：左侧 `4px × 20px` 蓝色竖条 `#4A90E2`（圆角 2px），右侧 8px 间距后为 `18px bold #333` 标题文字。标题中的关键数字使用 `#4A90E2` 蓝色高亮。例如：`| 本周学习 4 个知识点，刷了 34 道题`。"
- "创建环形进度图：直径 80px，环宽 10px，填充色 `#F5C542`（金黄），底环 `#F0F0F0`。中心显示 `28px bold #333` 百分比数字 + `%`。环下方 `12px regular #999` 标签。"
- "创建双色柱状图：金黄 `#F5C542` 代表"我的"，蓝色 `#4A90E2` 代表"洋葱平均"。柱宽 28px，同组间距 4px，顶部圆角 4px。数值标签 `12px #333` 在柱顶上方。X 轴 `14px #666`。左上角圆点图例。"
- "创建鼓励语气泡：白色背景，`1px solid rgba(74, 144, 226, 0.3)` 蓝色描边，`border-radius: 12px`，`padding: 16px 20px`。文字 `14px regular #333`，居中，亲切语气（'最近答题正确率稳中有升，不错嘛~'）。"
- "创建黄色胶囊按钮：`background: #FFD426`，`color: #333`，`font-size: 16px; font-weight: bold`，`border-radius: 24px`，`padding: 10px 24px`。全宽变体 `width: 100%; padding: 14px 0`。"
- "创建知识点列表卡片：蓝色虚线描边 `1px dashed rgba(74, 144, 226, 0.4)`，`border-radius: 12px`。顶部鼓励语。每个列表项为知识点名称（左，`14px #333`）+ 黄色胶囊按钮'去学完'（右）。项间 `1px solid #F0F0F0` 分隔。"

### Iteration Guide
1. **洋葱蓝 `#4A90E2` 是唯一品牌色**——所有强调、链接、图表平均线、描边均使用此色或其透明变体。不要引入其他品牌色
2. **数字是页面主角**——KPI 数字必须是视觉层级最高的元素（最大字号 + 粗体），标签和单位必须弱化
3. **趋势三色不可混用**——绿色仅表上升、红色仅表下降、橙色仅表持平。不要将这些颜色用于其他用途
4. **金黄 `#F5C542` = "我的"，蓝色 `#4A90E2` = "平均"**——所有对比图表严格遵循此映射，不可交换
5. **卡片阴影极浅**——`0 2px 8px rgba(0,0,0,0.06)` 是最大阴影，不要使用更深的阴影。层次靠背景色差和描边建立
6. **鼓励语气泡用蓝色描边而非阴影**——`1px solid rgba(74, 144, 226, 0.3)` + `border-radius: 12px`
7. **黄色按钮用于行动号召**——"去巩固"、"去学完"、"分享给朋友" 都使用 `#FFD426` 胶囊按钮，文字深色 `#333`
8. **章节标题格式严格统一**——`蓝色竖条 | + 粗体文字 + 关键数字蓝色高亮`，不要使用其他标题样式
9. **学科颜色取自官方色板**——饼图、分类标签等涉及学科的颜色严格使用 Subject Color Palette 中对应色值
10. **移动端优先，单列布局**——不要使用多列并排布局（KPI 3 列网格除外），保持垂直流
